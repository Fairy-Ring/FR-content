// Pest Control — Cross onto the waiting lander TOPSIDE (open boat floor).
// L1 is only the plank loc (midair). There is no L1 deck. Dest is L0
// pest_lander_boat_floor occupancy. Ladder 14314 is Climb-out later, not dest.
// Stand 2657,2639 L0. Dest 2660,2640 L0.

[oploc1,pest_lander_gangplank]
p_telejump(0_41_41_36_16); // 2660,2640 L0 — lander well / topside
mes("You board the waiting lander. The boat will leave when enough volunteers have boarded.");
