// Dwarf Cannon — Go-through cave tunnel -> Arzinian mine 2780,10161 L0.
// Gate %dwarfrock_quest >= 10 (mine phase; start 10, complete 110). Dest
// CANDIDATE 2780,10161 L0 — first walkable W of the tunnel loc 5973
// @ 2781,10161. Never dest onto the loc; never the demon 2367,4960 L3.
// Cite dest-uncited-pass-next-377.md + dest-from-our-maps-remaining-377.md.

[oploc1,dwarf_cavewall_tunnel]
if (%dwarfrock_quest < 10) {
    ~displaymessage(^dm_default);
    return;
}
p_telejump(0_43_158_28_49); // 2780,10161 L0 — walkable W of 2781,10161
